/**
 * Percorso scelto dal collaboratore.
 *
 * NONE      -> nessun percorso selezionato
 * STANDARD  -> percorso graduale
 * IDENTITY  -> percorso intensivo
 */
export enum JourneyPath {
  NONE = "NONE",
  STANDARD = "STANDARD",
  IDENTITY = "IDENTITY",
}