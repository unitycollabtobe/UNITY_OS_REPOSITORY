// ===========================================
// UNITY OS
// User Role
// ===========================================

/**
 * Ruoli disponibili all'interno di Unity.
 *
 * GUEST         -> visitatore
 * COLLABORATOR -> collaboratore
 * SPEAKER      -> relatore autorizzato
 * ADMIN        -> amministratore
 */
export enum UserRole {
  GUEST = "GUEST",
  COLLABORATOR = "COLLABORATOR",
  SPEAKER = "SPEAKER",
  ADMIN = "ADMIN",
}